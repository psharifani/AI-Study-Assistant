OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
# Used when uploading images and when a PDF has almost no embedded text (scanned/handwritten pages are rasterized). Must be vision-capable.
OPENAI_VISION_MODEL=gpt-4o-mini
