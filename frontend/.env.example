# Backend URL for the dev-server proxy. If uvicorn fails on port 8000 with WinError 10013,
# start the API on another port and set the same URL here, then restart `npm run dev`.
# Example:
# VITE_API_PROXY=http://127.0.0.1:8080
